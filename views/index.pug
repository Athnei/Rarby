html
  head
    title Rarby
  body
      ul
        for item in torrents
         li
         - url = item.magnetLink
          a(href=url) #{item.title}
          - imgUrl = item.imgLink
            img(src=imgUrl, referrerPolicy="no-referrer")